task: naive-dma256-depth
status: complete
phase: complete
pitfalls:
  - "Depth-only cache lane FIFOs misassemble out-of-order responses; all failed numerical runs are excluded from performance selection."
  - "DMA perf.rd_bytes counts full aggregate beats, including masked lanes; use byte-enabled and physical-lane traffic separately."
  - "Effective reorder depth is capped by the existing transport tag namespace; requested D128/R32 has 64 slots."
  - "SystemVerilog loop-local initializers are static; assign per-iteration scoreboard counts explicitly."
  - "fsdbreport needs one -s followed by all paths for batching; repeated -s only retains the final path."
  - "Two baseline simulations were already running at the authorized source transition; captured runtime artifact hashes and exact prior cycles attest their immutable executions."
entries:
  - timestamp: "2026-09-14 15:35"
    action: "Captured pre-edit RTL and config provenance; implement depth-only parameterization and frozen VCS sweeps."
  - timestamp: "2026-09-14 15:41"
    action: "L16 M4 PASS with exact pre-edit cycles (16257 GEMM / 22854 core). Corrected TB static loop-local count initialization and made final throughput phase all reads; retained attempt1 logs."
  - timestamp: "2026-09-14 15:42"
    action: "All 10 masked splitter tests passed: 4x64B and 32x8B at depths 8/16/32/64/128, including context-full and >=16 consecutive output cycles. Started isolated D256 sweep; wrapper chooses available socket ports."
  - timestamp: "2026-09-14 15:45"
    action: "D256 D8/R32 M4 numerical FAIL (1937 mismatches); source hashes frozen. DMA unit 256B/cycle both directions PASS. Exclude failed cycles; investigate cache response ordering before performance adoption."
  - timestamp: "2026-09-14 15:55"
    action: "User authorized response reordering. Implemented opt-in private-tag lane SRAM stores and in-order reassembly; 10 adversarial response-order candidate tests PASS. Ongoing baseline executables/runtime/kernel hashes captured before source replacement; old schedulers paused."
  - timestamp: "2026-09-14 16:02"
    action: "Ordered D256 M4 PASS at D8/16/32/64 R32. Core cycles 21381/19131/19206/19206; L32 baseline 19629. M256 simulations run in isolated builds. Full DMA misalignment suite 2126/2126 PASS; 256B/cycle unit throughput PASS both directions."
  - timestamp: "2026-09-14 16:14"
    action: "All seven ordered M4 configurations PASS; all 26 production splitter tests PASS. Improve M4/M256 and L16 M4/M256 exactly match prior cycles. Batched FSDB analysis confirms D8 context saturation and unchanged M4 performance from D32 upward; M256 sweep continues."
  - timestamp: "2026-09-14 16:28"
    action: "Final guarded candidate passes all 29 splitter tests, including UUID restoration, debug trace compilation, and non-naive legacy mode. Logic-token identity excludes only the unused INSTANCE_ID debug label; helper declaration and instantiation are absent from improve preprocessing. Await frozen M256 sweeps before final source/config application."
  - timestamp: "2026-09-14 16:56"
    action: "All 18 naive M4/M256 cases PASS; improve M4/M256 PASS with zero cycle delta. Selected D8/R32 by smallest-depth-within-1%-of-best-M256 rule: 417981 core cycles versus L32 421479 and best 417606. D16/R32 is better for M4 (19131) and M256 DMA activity (48392 versus D8 65348)."
  - timestamp: "2026-09-14 16:56"
    action: "Applied final guarded RTL only after sweeps completed. Final RTL SHA matches 29/29 tested candidate; logic identity preserved except unused debug label, improve 570/570 preprocessing checks PASS, final config flags exactly match selected run, original L16 unchanged. Results, CSV, plots, and reproduction notes saved. No synthesis or commits performed."
  - timestamp: "2026-09-14 17:25"
    action: "User requested commits. Committed RTL/config changes as 2da49907; package regression tests, experiment tools, source snapshots, and measured results in a separate verification commit. Rechecked tested RTL/TB hashes, syntax, original snapshots, and identity records; no RTL changes or simulation reruns needed during commit preparation."
  - timestamp: "2026-09-14 17:26"
    action: "Staged whitespace check flagged historical snapshot whitespace, Matplotlib SVG path whitespace, and CSV CRLF records. Added narrowly scoped task attributes to preserve original evidence bytes and generated formats; RTL and executable-source whitespace checks remain enabled."
