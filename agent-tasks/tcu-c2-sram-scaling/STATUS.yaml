task: tcu-c2-sram-scaling
status: completed
updated: "2026-07-15 14:12"
summary: "Mapped SRAM hierarchy and scaling for tcu_th16_c2; LMEM_LOG_SIZE=19 preserves aggregate Local Mem versus a one-core LMEM_LOG_SIZE=20 configuration, while per-core GPR and small RAMs still grow."
attempts:
  - timestamp: "2026-07-15 14:06"
    action: "Read the target config and identified NUM_CLUSTERS=1, NUM_CORES=2, NUM_THREADS=16, L2_ENABLE, and LMEM_LOG_SIZE=20."
    result: "Target configuration captured; RTL capacity and replication analysis is in progress."
  - timestamp: "2026-07-15 14:12"
    action: "Traced socket, cluster, core, cache, local-memory, and GPR RAM instantiations and checked the current PnR log."
    result: "I-cache and D-cache are per socket, L2 is per cluster, Local Mem and GPR are per core. One and two cores both use one socket; changing LMEM_LOG_SIZE from 20 to 19 keeps aggregate Local Mem at 1 MiB. The current run also appends NUM_CORES=1 and fails before synthesis."
pitfalls:
  - "Matching the four large configured memories does not make physical BRAM/URAM utilization exactly equal because GPR, per-core control RAMs, cache metadata, and primitive packing also contribute."
  - "The XRT Makefile appends a core configuration from the NUM_CORES make variable; the PnR invocation must set NUM_CORES=2 to avoid a conflicting -DNUM_CORES=1."
