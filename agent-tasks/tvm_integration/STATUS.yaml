task: tvm_integration
state: ACTIVE
spec: vortex_tvm_llama3_8b_synthetic_inference_plan.md
current_phase: llama3_synthetic_inference_s1_alone_no_retry_complete
updated_at: "2026-08-30 04:56"
repositories:
  vortex:
    path: /home/jaeyongjang/project.local/vortex_base
    branch: fpint
    baseline: 08121831
    final_head: fc55b00f
  tvm:
    path: /home/jaeyongjang/project.local/tvm
    branch: fi_system
    baseline: 089a098a2
    final_head: aee6d008c
progress:
  - timestamp: "2026-08-30 04:56"
    phase: llama3_synthetic_inference_s1_alone_no_retry_acceptance
    status: pass
    summary: Replaced the intermittently corrupt pairwise MLP Hadamard chain with an equivalent dense mixed-radix transform and passed retry-free persistent prefill plus three-step decode with device-resident state on the pinned U55C.
    learned: Checkpoint captures placed the first bad value at transformed_mlp; a representative final butterfly stage jumped from max 44.89 to 4407.50 while K/V stayed exact. The normal eight-output dense package passed alternating layers 24/25 for 100/100 calls, one full chain, two persistent host-snapshot chains, and two persistent production device-copy chains. The final device-copy run covered 256 decoder calls with retry 0, nonfinite 0, exact cache lengths 1/2/3/4, identical step hashes, 1028 D2D state copies per repetition, and zero cache host snapshots. No RTL or xclbin change was required.
  - timestamp: "2026-08-30 02:43"
    phase: llama3_synthetic_inference_divergence_reduction
    status: pass_with_diagnostic_recovery
    summary: Separated apparent decode divergence into quantized-chain drift and transient accelerator-call corruption, then passed physical embedding/head prefill plus three decode steps twice in one persistent U55C process using checked identical-input retries.
    learned: Exact-input PyTorch replay of the decode-3/layer-1 capture matched hardware at hidden relative-L2 0.0005878 and cosine 0.999999827, while the canonical chain differed by relative-L2 0.25999; later canonical drift is therefore W4/K4/V4 cache accumulation, not a local GEMM failure. The persistent physical run generated [89754, 29229, 89754] in both repetitions with exact 1/2/3/4 cache lengths. Four and seven transient events were recovered respectively, at most retry 2, with no bad hidden propagation. The end-to-end diagnostic goal passes, but production acceptance still requires finding the internal accelerator corruption cause and passing with retries disabled.
  - timestamp: "2026-08-29 23:22"
    phase: llama3_synthetic_inference_persistent_process
    status: blocked
    summary: Added persistent-process inference repetition with one device open/xclbin programming event and one resident 5.7 GB archive; three consecutive prefills passed, but both prefill-plus-decode repetitions failed in decode 1.
    learned: The persistent prefill run passed 3/3 at 92.33-95.10 seconds with one XRT initialization and identical parameter addresses. In a two-repetition decode3 run, repetition 0 failed decode-1 hidden at layer 2 with relative-L2 729.49, while repetition 1 passed all decode-1 hidden checkpoints and failed layer-1 key-cache semantic validation at relative-L2 0.08173. Process restart and FPGA reprogramming were prefill confounders, not the decode root cause; next isolate final-head/embedding/Vortex-program transitions while keeping the xclbin and archive resident.
  - timestamp: "2026-08-29 19:40"
    phase: llama3_synthetic_inference_address_reduction
    status: blocked
    summary: Added physical-address diagnostics and fixed-context controls; exact layers pass at fixed, swept, alternating, and above-4GB addresses, while the full sequential chain remains non-repeatable.
    learned: A single bad address and 32-bit truncation are ruled out. Alternating fixed decode layers 11/24 passed 40/40. Reproducing the runner's state copies poisoned a later raw VM result; key-scale-only copy was the shortest transfer trigger, but removing every copy still allowed a sequential prefill failure. One fixed-hidden prefill passed 32 layers and a fresh fixed-hidden run failed at layer 3, so address fixation is not a sufficient fix. Next isolate read/write/allocation effects, cold-versus-warm device state, and the shortest data-dependent layer prefix before returning to RTL.
  - timestamp: "2026-08-29 18:30"
    phase: llama3_synthetic_inference_s1_alone
    status: blocked
    summary: Implemented real-geometry partitioned packaging, reload, deterministic 5.7 GB archive binding, eager references, and the inference CLI, but repeatable S1/alone hardware execution is blocked by variable-layer non-finite output on the pinned U55C.
    learned: One complete 32-layer prefill passed with logits relative-L2 0.000309 and top-1 agreement 1.0, but the same path is not repeatable. An exact repeated-layer reproducer eventually returns a wholly non-finite hidden tensor; only AP reset plus a 50 ms settle before every low-level launch survived 20 repetitions, while shorter/periodic resets and handshake variants failed. This satisfies the plan's reduced-reproducer condition for a follow-up AFU launch-completion and AXI write-drain investigation. No RTL, xclbin, or production reset workaround was retained.
  - timestamp: "2026-08-29 04:34"
    phase: llama3_milestone_6
    status: pass
    summary: Passed the complete real-geometry 32-layer decoder stack through prefill and three consecutive decode steps under both alone and fused on the pinned U55C.
    learned: A monolithic 32-layer Relax build did not finish its first executable after 76 minutes, so the accepted path compiles one layer once and reuses one VM over 32 global archive slices. Explicit Vortex D2D copies stabilize reusable VM outputs without host round trips; prefill caches then update in place. Final alone/fused runs passed in 802.14/805.11 seconds with a 4,362,600,448-byte resident archive and about 23.8 GB peak RSS. The remaining milestone is the full token-to-logits S1-S4 matrix.
  - timestamp: "2026-08-29 01:03"
    phase: llama3_milestone_6
    status: partial_pass
    summary: Scaled the real-geometry semantic-cache chain from two to four decoder layers and passed prefill plus three decode steps under both alone and fused on the pinned U55C.
    learned: Four-layer alone/fused completed in 536.24/539.74 seconds, reused a 545,325,056-byte external archive, and peaked near 20.9 GB RSS. Prefill took 11.04/10.78 seconds and the three decode steps took about 10.95-11.61 seconds. The next stack gate is all 32 real layers; final token-to-logits S1-S4 hardware acceptance remains after that.
  - timestamp: "2026-08-29 00:37"
    phase: llama3_milestone_6
    status: partial_pass
    summary: Accepted real-geometry two-layer prefill plus three-step decode under alone and fused using a stage-aware multi-layer FP16 and semantic KV-cache rule while preserving the stricter one-layer rule.
    learned: Small FP16 values now use absolute error and large values use relative error, guarded by relative-L2 and cosine. Dequantized K/V valid prefixes are checked alongside bounded one-code payload/zero differences, exact cache length, and untouched suffixes. The final alone/fused runs passed in 241.86/253.92 seconds with a 272,662,528-byte resident archive; real four- and 32-layer scaling remain.
  - timestamp: "2026-08-28 23:26"
    phase: llama3_milestone_6
    status: active
    summary: Brought up real-geometry two-layer external-prepacked execution under alone and fused, but retained a numerical acceptance failure instead of weakening the one-layer threshold.
    learned: Both policies completed physical prefill with the same result, but layer-two V cache had 21/2048 one-code differences (1.0254%) and K/V scale differences reached 0.00195/0.00220 versus eager. The common result points to layer-to-layer FP16 divergence amplified by KV requantization rather than fused-layout corruption. A per-layer dequantized-cache and hidden checkpoint policy is required before scaling to 32 real layers.
  - timestamp: "2026-08-28 23:10"
    phase: llama3_milestone_7
    status: partial_pass
    summary: Added token embedding, final RMSNorm, asymmetric W4 LM head, external full-model archive records, and token-to-logits prefill plus three-step decode; bytecode and compiled VM export/reload passed alone and fused on the two-layer smoke geometry.
    learned: Real Llama3-8B 32-layer prefill and decode both strict-exported on meta in about 28 and 27 seconds with 13,761 and 13,704 graph nodes. The physical two-layer smoke used one 262,144-byte resident archive; reloaded VM prefill took about 1.1-1.2 seconds and decode steps about 1.0-1.3 seconds. Full 32-layer real-geometry hardware acceptance remains gated by Milestone 6 numerical policy and scaling.
  - timestamp: "2026-08-28 22:55"
    phase: llama3_milestone_6
    status: partial_pass
    summary: Added layer-major stack state, versioned physical C4 parameter archives, external-prepacked W4A16 lowering, and resident one-time upload reuse; two- and four-layer prefill plus three decode steps passed alone and fused on U55C smoke geometry.
    learned: The external prepacked adapter removes runtime W/scale/zero layout kernels and passed a direct arbitrary-shape U55C GEMM. Two-layer smoke build/run was roughly 25 seconds and 1.2 seconds per invocation; four-layer was roughly 44 seconds and 2.3-2.5 seconds. Archive validation rejects profile, layer-count, size, and hash mismatches before upload.
  - timestamp: "2026-08-28 22:23"
    phase: llama3_milestone_3_5
    status: pass_with_limited_fusion
    summary: Completed S1-S4 prefill plus three decode steps for alone and fused on the pinned U55C, with in-place KV append, runtime capacity assertion, and stable cache tensors.
    learned: Fused lowering reuses three shared GEMM-A layouts per layer (15 transforms reduced to 12), but the broader RMSNorm/RoPE/Hadamard/cache-layout propagation described by Milestones 3 and 5 is not yet implemented. S4 measured 16/14,336 one-code K payload differences, motivating the explicit max(4, ceil(0.2%)) cross-backend cache rule. S4 fused also rejected cache_length equal to capacity without modifying cache state.
  - timestamp: "2026-08-28 21:07"
    phase: llama3_milestone_4
    status: active
    summary: Added a physical one-layer prefill-plus-three-step decode chain that passes prefill cache tensors directly into each next Relax VM invocation and checks every step's semantic output plus exact cache prefix/suffix preservation.
    learned: The first S1/alone chain attempt timed out inside the prefill portion before reaching dynamic decode, even though the same prefill specialization passes alone. A reduced-timeout launch-traced rerun is required to distinguish an intermittent long multi-kernel hardware stall from executable co-residency or binary-switch behavior before attributing the failure to persistent cache lowering.
  - timestamp: "2026-08-28 21:07"
    phase: llama3_milestone_2
    status: pass
    summary: Completed the real-geometry S1-S4 C4/alone prefill bring-up and fixed Vortex infinity emission without changing the backend-neutral causal-mask model.
    learned: S2 passed the final rebuilt codegen path in 92.02 s with 71.19 s build and 5.89 s execution. S3 and S4 completed batch-two/GQA execution in 149.49 s and 179.49 s. Cross-backend dynamic KV quantization differed only at one INT4 code for 1/3/11 nibbles across S3/S2/S4 while hidden, scales, zero-points, lengths, and untouched suffixes passed; the plan now distinguishes identical-input byte-exact quantization from stage-aware end-to-end payload comparison capped at a 0.1% one-code mismatch rate.
  - timestamp: "2026-08-28 20:45"
    phase: llama3_milestone_2
    status: active
    summary: Imported and lowered the real-geometry one-layer Llama3 graph, added static rank-5 GQA decomposition into checked rank-2 IMPROVE ABI v2 submissions, enabled 32/64/128 qblocks, and passed S1 C4/alone on the pinned physical U55C with compile-time-prepacked constants.
    learned: 'S1 produced exact K/V payload, scale, zero-point, and length state with a 0.00048828125 maximum hidden-state error. Binding deterministic weights before lowering avoids a prohibitively slow runtime layout path and reduced S1 to 66.88 s build plus 2.62 s execution. The first S2-S4 attempt exposed two independent gaps: literal negative infinity emits invalid `-inff` Vortex C++, and batch-two S3 reaches a device timeout that now requires launch-level isolation. QBLK=128 failed until the kernel ABI v2 guard was extended from 32 to the compiler-supported 32/64/128 set; direct ABI and Relax VM QDIR=1 coverage then passed.'
  - timestamp: "2026-08-28 20:05"
    phase: llama3_milestone_1
    status: pass
    summary: Added backend-neutral all-asymmetric Llama3 one-layer prefill/decode modules, a runtime-scalar logical cache append, fixed-capacity GQA attention, and S1-S4 real-geometry torch.export coverage.
    learned: The same decode ExportedProgram shape can carry a scalar cache length through three functional cache updates. Tiny eager tests match the independent layer-accuracy schedule and preserve byte-exact W/K/V payload, FP16 scale, and INT16 zero-point state. Seven focused export tests pass, including all four real Llama3 geometry cases on meta tensors, with no physical C4 op names in the graphs.
  - timestamp: "2026-08-28 19:48"
    phase: llama3_milestone_0
    status: pass
    summary: Versioned the all-asymmetric C4 alone/fused workload variants, updated the eager W/K/V parameter and persistent-cache contracts, and froze the target plus S1-S4 workload hashes in a baseline manifest.
    learned: Historical SpinQuant variants remain symmetric for W/V, while the new signed_all_asymmetric_wkv4_v1 policy uses FP16 scales and INT16 zero-points for W, K, and V. The estimated complete parameter set is 5.741 GB and fits the pinned 34-bit target address space; host capacity is sufficient. Host regressions passed 87 tests plus 4 subtests, and all 38 workload-generator tests passed.
  - timestamp: "2026-08-28 19:28"
    phase: llama3_milestone_0
    status: active
    summary: Started the C4-only PyTorch-to-TVM Llama3-8B plan with existing GEMM RTL, FSM, MMIO, ABI v2, and the pinned U55C xclbin fixed as non-modifiable dependencies.
    learned: The initial implementation must version the all-asymmetric W/K/V policy separately from historical SpinQuant variants, then establish exportable one-layer prefill/decode semantics before attempting C4 lowering or physical execution.
  - timestamp: "2026-08-28 02:07"
    phase: milestone_b_final_validation
    status: pass
    summary: Completed Milestone B with pre-legalization IMPROVE layout regions, descriptor-checked GEMM/vector chaining, compile-time constant prepacking, branch-local boundaries, and irregular multi-tile attention/KV-cache replay.
    learned: The final Vortex-focused host suite passed 206 tests with 72 hardware skips. The managed U55C improved-image suite passed all 37 selected hardware tests, including fused/unfused FFN, all WTRANS/QDIR constant-prepack combinations in bytecode and compiled VM modes, QK^T/PV attention, irregular capacity-129 KV-cache replay, export/load, and the existing Milestone A arbitrary-shape matrix. A final focused fused/unfused attention rerun passed both VM modes.
  - timestamp: "2026-08-28 01:58"
    phase: milestone_b_pipeline_split
    status: pass
    summary: Split lowering into a pre-legalization IMPROVE GEMM/layout-region pass and a post-fusion quantize/dequantize/KV-update pass.
    learned: Lowering every Vortex logical op before FuseOps/FuseTIR exposed scheduled non-root PrimFuncs inside generic fusion regions. Marking IMPROVE physical kernels opaque and delaying auxiliary tuple operators preserves graph-level GEMM fusion without violating FuseTIR's schedulable-root requirement.
  - timestamp: "2026-08-28 01:50"
    phase: milestone_b_hardware_environment
    status: pass
    summary: Moved physical validation into the managed Slurm U55C allocation after an unmanaged direct-XRT attempt found a busy card.
    learned: Direct XRT saw both U55C devices and selected a board whose xclbin was in use. The managed allocation plus xrt_device_detect.sh resolved the granted device deterministically and all subsequent physical runs passed.
  - timestamp: "2026-08-28 01:36"
    phase: milestone_b_baseline
    status: active
    summary: Started Milestone B layout-preserving graph execution from the completed arbitrary-shape GEMM baseline.
    learned: The existing lowerer already fuses a directly chained GEMM-C to GEMM-A and has row-major attention and KV-cache acceptance, but it still detiles across vector operators and retransforms W/scale/ZP at every invocation. Milestone B requires a pre-legalization graph pass, descriptor-aware tile-aware vector regions, branch-local boundaries, and constant prepacking.
  - timestamp: "2026-08-28 00:13"
    phase: final_validation
    status: pass
    summary: Fixed the TVM IMPROVE A-layout M-tail stride, added direct-ABI, K-group sentinel, boundary, QDIR, WTRANS, and multi-tile hardware regressions, and closed the physical U55C correctness blocker.
    learned: Native packs each K micro-tile with the real cur_m rows and reserves row-alignment padding only at the end of each (mt,kt) slot. The former TVM transform incorrectly used slot_m between micro-tiles, moving K=32 from element 224 to 256 for M=7. Direct ABI and sentinel tests pass after using cur_m for the payload stride while retaining slot_m for slot allocation. The focused host suite passed 123 tests with 32 skips, ten full Relax VM arbitrary-shape U55C cases passed, and native (7,31,33) passed.
  - timestamp: "2026-08-28 00:05"
    phase: root_cause
    status: pass
    summary: Reproduced the failure through the ABI v2 helper with host-generated physical buffers and compared the physical A layout against the passing native converter.
    learned: The failure was not caused by VM cache visibility, buffer lifetime, ABI validation, or MMIO fields. The TVM and initial debug-side A packers inserted M-row alignment padding between K micro-tiles, whereas hardware expects that padding only at the end of the containing (mt,kt) slot.
  - timestamp: "2026-08-28 00:01"
    phase: debug_isolation
    status: active
    summary: Started the arbitrary-shape IMPROVE GEMM correctness debug plan, preserving the minimal physical U55C Relax VM reproducer at logical shape (7,31,33).
    learned: Native Vortex GEMM and standalone TVM A/W/qparam transforms pass on U55C, while the chained Relax VM result omits the K=32 contribution; the first experiment will isolate the ABI v2 helper with host-generated physical buffers.
  - timestamp: "2026-08-20 16:50"
    phase: intake
    status: pass
    summary: Read the full implementation plan, resolved native serial execution, recorded both clean code baselines, and inspected the TVM and Vortex flows with CodeGraph.
    learned: The plan is a legacy multi-repository code plan; the TVM checkout is clean, while the Vortex checkout contains only the user-provided untracked plan.
  - timestamp: "2026-08-20 17:24"
    phase: 0
    status: pass
    summary: Made the TVM host build independent of the interactive Vortex LLVM environment, built TVM with host LLVM 18.1.8, restored the worktree-local Python FFI, passed 37 target tests, and passed vecadd on the pinned U55C fpint xclbin.
    learned: The authoritative hardware run used the manifest CONFIGS and completed with 26444 instructions, 46081 cycles, IPC 0.573859, and PASSED. This fork emits libtvm_compiler.so together with libtvm_runtime.so and libtvm_ffi.so.
  - timestamp: "2026-08-20 17:34"
    phase: 1
    status: pass
    summary: Registered vortex as a kDLExtDev GPU target with canonical hardware limits, XLEN-derived RISC-V triples, and focused parsing, validation, round-trip, and verifier tests.
    learned: The existing generic GPU verifier consumes max_threads_per_block without a Vortex-specific branch; a 129-thread block is rejected against the pinned 128-thread target capacity. Target suites passed with 46 passed and 9 skipped.
  - timestamp: "2026-08-20 17:47"
    phase: 2
    status: pass
    summary: Added a CodeGenC-based single-kernel Vortex source generator, target.build.vortex registration, source-capture compiler callback seam, shared ABI v1 header, and positive and rejection tests.
    learned: Fourteen Vortex codegen/target tests passed. The generated vecadd translation unit also passed the real Vortex clang syntax check with the pinned rv64imaf_zfh/lp64f toolchain configuration.
  - timestamp: "2026-08-20 18:01"
    phase: 3
    status: pass
    summary: Added an automatically registered, path-deterministic Vortex compiler callback that links generated C++ with the fpint LP64F profile and packages an uploadable vxbin.
    learned: The actual callback produced a non-empty image at VMA 0x180000000 even with an invalid ambient PATH. Compiler failures retain argv and stderr. Phase 1-3 suites passed with 19 tests.
  - timestamp: "2026-08-20 18:22"
    phase: 4
    status: pass
    summary: Added the Vortex runtime sidecar, serializable binary module, packed launch validation, kDLExtDev memory/copy API, Python device constructor, and callback-binary retention.
    learned: Twenty-five software tests passed with one hardware-only skip. The U55C allocation/copy/readback test passed in 7.87 seconds on index 0/BDF 0000:2a:00.1 with the pinned xclbin. Vortex StreamSync must be a no-op because ready_wait is a kernel-completion wait, not a generic fence. PackFuncVoidAddr is required to normalize Tensor handles before pointer ownership checks.
  - timestamp: "2026-08-20 18:34"
    phase: 5
    status: pass
    summary: Ran direct tvm.tirx.build vector addition through the normal host/device pipeline and validated five sizes and block configurations on the physical U55C.
    learned: The final hardware suite passed seven tests in 9.64 seconds; the last launch reported 41650 instructions, 64292 cycles, and IPC 0.647826. Vortex vxbin images reserve a fixed VMA, so DeviceAPI must serialize one active binary, reuse identical bytes, and evict a different image only after synchronous completion.
  - timestamp: "2026-08-20 18:44"
    phase: 6
    status: pass
    summary: Added serial-loop code generation and a flattened global-memory naive float32 matmul, then validated three shapes on the pinned U55C.
    learned: Software suites passed 30 tests with 9 hardware skips. Hardware matmul passed shapes 1x1x1, 2x3x4, and 3x5x7 at block sizes up to 128; the last run reported 33324 instructions, 65951 cycles, and IPC 0.505284. Hardware Python runs require the Vortex runtime directory in LD_LIBRARY_PATH so libvortex-xrt.so is discoverable.
  - timestamp: "2026-08-20 18:57"
    phase: 7
    status: pass
    summary: Added deterministic multi-kernel IDs, per-kernel ABI decoders, a native main dispatcher, serialized name-to-ID metadata, and a two-kernel direct-runtime test.
    learned: The U55C invoked both functions from one binary successfully and reported 36410 instructions, 68390 cycles, and IPC 0.532388. The complete post-review software suite passed 33 tests with 10 hardware-only skips; obsolete single-kernel assertions were updated to the dispatcher contract.
  - timestamp: "2026-08-20 19:12"
    phase: 8
    status: pass
    summary: Integrated Vortex modules into Relax VM linking and ran a legalized add-then-multiply graph in bytecode and compiled VM modes, including executable export and reload.
    learned: The pinned U55C Phase 8 suite passed all four tests with six physical launches. Bytecode instrumentation observed add then multiply; compiled VM bypasses that instrumentation but produced the same order-sensitive NumPy result. Available Vortex memory stayed stable across original and reloaded executions.
  - timestamp: "2026-08-20 19:31"
    phase: 9
    status: pass
    summary: Exported a frozen Linear-ReLU-Linear PyTorch MLP, bound its four parameters into Relax, compiled it with the Vortex pipeline, and ran dynamic batches in both Relax VM modes on U55C.
    learned: The Vortex-only Relax pipeline uses DLight gpu.Fallback to produce supported one-dimensional schedules; generic Matmul otherwise emits unsupported blockIdx.y/shared-local constructs. Batch 1 and irregular batch 3 matched eager PyTorch. Final bytecode/compiled artifacts were 73864/79424 bytes and measured roughly 6.8-7.5 ms per synchronous host call.
  - timestamp: "2026-08-20 20:03"
    phase: 10
    status: pass
    summary: "Completed the planned capability and quality pass: Vortex-aware Relax scheduling, dynamic-batch PyTorch execution, runtime metrics, scalar ABI coverage, bounded compiler and kernel waits, reusable launch packets, chunked device copies, and strict hardware-environment validation."
    learned: Two- and three-dimensional bindings, shared/local allocation, and barriers remain explicit unsupported boundaries rather than incomplete acceptance work. Relax VM bytecode remains the reference execution mode because compiled mode did not provide a material latency advantage; no AOT path was added.
  - timestamp: "2026-08-20 20:03"
    phase: final_validation
    status: pass
    summary: Applied the final simplify and code-review findings, rebuilt TVM, passed the complete focused software regression, and reran all Vortex runtime, TIRx, Relax VM, and torch.export tests on the physical U55C.
    learned: The final exact-workspace hardware run passed 34 tests in 16.62 seconds on XRT index 0/BDF 0000:2a:00.1 with the pinned xclbin and manifest CONFIGS. The torch.export MLP remained correct in bytecode and compiled modes; measured host calls were 6.90-7.41 ms and final hardware PERF was 33750 instructions, 60854 cycles, IPC 0.554606.
  - timestamp: "2026-08-20 20:21"
    phase: review_validation
    status: pass
    summary: Validated kernel timeout configuration before launch and expanded the physical device-to-device copy test across the one MiB staging boundary.
    learned: The final runtime-focused host suite passed 27 tests with one expected hardware skip, and the pinned U55C runtime suite passed all 11 tests in 8.95 seconds on index 0/BDF 0000:2a:00.1.
pitfalls:
  - Pre-scheduled IMPROVE PrimFuncs introduced before FuseOps must carry opaque op_pattern metadata; otherwise an adjacent logical quantize or tuple region can make FuseTIR attempt to fuse a non-root scheduled body.
  - Physical TVM tests must run inside the managed Slurm U55C allocation and resolve XRT_DEVICE_INDEX from ci/xrt_device_detect.sh; an unmanaged process may select a busy board even when another U55C is available.
  - The user-provided plan was untracked before implementation and must not be included accidentally in an implementation commit.
  - Host TVM LLVM and the Vortex device LLVM must remain isolated; ambient shell PATH is not authoritative.
  - Hardware execution is authoritative. simx may only diagnose a hardware failure and cannot replace a U55C result.
  - The tvm-ffi submodule must be initialized. Its Python module is skipped when tvm-ffi is included as a CMake subproject, so build.sh configures a standalone local Python FFI build without an editable install.
  - LLVM 18 reports a raw libxml2 dependency; this host requires a deterministic library directory. DNNL is unavailable and remains disabled.
  - A full xclbin path does not supply Vortex CONFIGS. Ambient stale CONFIGS caused vx_ready_wait to hang, while the pinned manifest CONFIGS passed; hardware runs must export that exact value.
  - The Vortex build tree needed configure-generated ci/blackbox.sh before ci/run_black.sh could run. The resulting untracked build products are not source changes and must not enter commits.
  - The current upstream vxbin.py uses a fixed /tmp/temp_kernel.bin scratch path, so compiler packaging must remain serial until that helper is made concurrency-safe.
  - The local build helper and runtime sidecar intentionally use this workspace's explicit Vortex root and source-tree runtime rpath. A relocatable install/package contract is future deployment work, not part of the validated local U55C flow.
  - The independent Claude adversarial review route could not authenticate in its execution context, so the adversarial lens used an in-process reviewer and has no cross-model corroboration receipt.
  - TIR script closure constants must be explicitly captured before constructing a PrimFunc; the first post-fix Relax build exposed a missing dma_kt closure variable before any hardware launch.
  - U55C Python runs require VORTEX_DRIVER=xrt, XRT_XCLBIN_PATH, and the configured Vortex build/runtime directory in LD_LIBRARY_PATH; otherwise the runtime rejects device open before computation.
blockers: []
