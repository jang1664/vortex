# `mem/` 디렉터리 요약

`mem/` 디렉터리는 시스템의 메모리 서브시스템(시뮬레이터용 드라이버, 모델, 테스트벤치 지원 등)을 포함할 가능성이 큽니다. 구체적 파일은 디렉터리 내부를 확인하세요.

역할
- 메모리 모델, 시뮬레이션용 메모리 백엔드, 또는 테스트용 메모리 스텁을 제공하여 상위 모듈의 메모리 요청을 처리합니다.

다음 학습 포인트
- `Vortex.sv`의 `mem_bus_if`가 외부 메모와 어떻게 인터페이스되는지 추적하세요. 메모 모델(시뮬레이션/실제 HW)에 따라 동작이 달라질 수 있습니다.
