// The device algorithm is identical to opt.  This tag pairs it with the
// corrected one-warp-per-row host launch so the launch-policy experiment stays
// independently selectable and directly comparable.
#include "kernel.opt.cpp"
